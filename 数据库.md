database:syj

#许愿那两个

```mysql
CREATE TABLE IF NOT EXISTS `hope`(
    `ID` INT UNSIGNED AUTO_INCREMENT,
    `NEGAI`VARCHAR(200) NOT NULL,
    `IFHELP`VARCHAR(30) NOT NULL,
    `TIMES`INT,
    `contact`VARCHAR(50) NOT NULL,
    `IfGiveUp`INT,
    `Tegami`INT,
    `Name`VARCHAR(20)NOT NULL,
    `Description`VARCHAR(150) NOT NULL,
    `Extent`VARCHAR(50) NOT NULL,
    PRIMARY KEY(`ID`)

)ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

#抽奖页面

```mysql
CREATE TABLE IF NOT EXISTS `cj`(
    `ID` INT UNSIGNED AUTO_INCREMENT,
    `Times`INT NOT NULL,
    `Name`VARCHAR(20)NOT NULL,
    `Xueyuan`VARCHAR(20)NOT NULL,
    `Tel`VARCHAR(20)NOT NULL,
    `Address`VARCHAR(50)NOT NULL,
)ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

​    

)ENGINE=InnoDB DEFAULT CHARSET=utf8;

###图鉴页面

```mysql
CREATE TABLE IF NOT EXISTS `pic`(
    `ID` INT UNSIGNED AUTO_INCREMENT,
    `Food`INT NOT NULL,
    `F1`INT NOT NULL,
    `F2`INT NOT NULL,
    `F3`INT NOT NULL,
    `F4`INT NOT NULL,
    `F5`INT NOT NULL,
    `F6`INT NOT NULL,
    `F7`INT NOT NULL,
    `F8`INT NOT NULL,
    

)ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

###广场页面

```mysql
CREATE TABLE IF NOT EXISTS `Square`(
    `kksk` INT UNSIGNED AUTO_INCREMENT,
    `Content`VARCHAR(200) NOT NULL,
    `Name`VARCHAR(20)NOT NULL,
    `serverid`VARCHAR(20)NOT NULL,
    `offset`INT NOT NULL,
    `limit`INT NOT NULL,
    `ifLike`INT NOT NULL,
    `id`VARCHAR(20) NOT NULL,
    `Like`INT NOT NULL,
    `ifLiked`INT NOT NULL,
    PRIMARY KEY(`kksk`)

)ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

